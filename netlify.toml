[build]
  publish = "./"                      # Static files in root
  functions = "netlify/functions"     # Serverless functions directory

[build.environment]
  NODE_VERSION = "18"                 # Force Node 18 for PDFKit + get-stream

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
