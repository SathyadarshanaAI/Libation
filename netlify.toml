[build]
  publish = "./"                    # Use "./" for static sites at root
  functions = "netlify/functions"   # Where your Netlify functions live

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
